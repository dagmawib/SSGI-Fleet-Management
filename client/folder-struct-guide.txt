Guide for project folder Structure
---

## **📂 Simple & Beginner-Friendly Folder Structure**
```
📂 project-root/
 ├── 📂 android/               # Native Android files (Generated)
 ├── 📂 ios/                   # Native iOS files (Generated)
 ├── 📂 lib/                   # Main Flutter app code
 │   ├── 📂 models/            # Data models (User, Post, etc.)
 │   ├── 📂 screens/           # App screens (Login, Home, Profile)
 │   ├── 📂 widgets/           # Reusable UI components (Buttons, Cards)
 │   ├── 📂 services/          # API calls, Firebase, Database helpers
 │   ├── 📂 state/             # State management (Provider, Bloc, etc.)
 │   ├── 📂 utils/             # Helper functions, formatters, constants
 │   ├── main.dart             # App entry point
 ├── 📂 assets/                # Images, fonts, and icons
 │   ├── images/               # Store all images here
 │   ├── fonts/                # Custom fonts
 ├── 📂 test/                  # Unit & widget tests
 ├── 📜 pubspec.yaml           # Dependencies & assets list
 ├── 📜 README.md              # Project instructions for your team
```

---

## **💡 Explanation (Beginner-Friendly)**
### **1. `models/` – Data Models**
   - Stores **data structures** used in the app.  
   - Example: `user_model.dart`, `post_model.dart`.

### **2. `screens/` – App Pages**
   - Each **page of your app** has its own file here.  
   - Example:
     ```
     screens/
     ├── login_screen.dart
     ├── home_screen.dart
     ├── profile_screen.dart
     ```

### **3. `widgets/` – Reusable UI Components**
   - Stores small UI components **used in multiple places** (e.g., buttons, cards).  
   - Example:
     ```
     widgets/
     ├── custom_button.dart
     ├── profile_card.dart
     ```

### **4. `services/` – API & Database Calls**
   - Handles **Firebase, API requests, and local storage**.  
   - Example:
     ```
     services/
     ├── auth_service.dart  # Login & Signup functions
     ├── database_service.dart  # Fetch/save data
     ```

### **5. `state/` – App State Management**
   - Stores **state management logic** (Provider, Bloc, Riverpod, etc.).  
   - If using **Provider**, example:
     ```
     state/
     ├── auth_provider.dart
     ├── user_provider.dart
     ```

### **6. `utils/` – Helper Functions & Constants**
   - Stores **small reusable functions & constants**.  
   - Example:
     ```
     utils/
     ├── app_constants.dart  # API keys, app colors, etc.
     ├── helpers.dart  # Helper functions
     ```

### **7. `assets/` – Images & Fonts**
   - Stores **app icons, images, and fonts**.  
   - Example:
     ```
     assets/
     ├── images/  # App images
     ├── fonts/   # Custom fonts
     ```

### **8. `test/` – Testing Folder**
   - Stores **unit & widget tests** for your app.

